# yield.fm copy positioning v1

Level 0/1 TRIBE batch.

- Anchors are reusable reference texts.
- Candidates are 30-80 word hero variants.
- One `.txt` file equals one TRIBE job.
- Metadata lives in `manifest.json`; text files contain only copy seen by the model.

Analysis target:

```txt
+ DeFi-native yield product
+ institutional finance credibility
+ music royalty specificity
-- generic Web3 / creator-economy language
```
